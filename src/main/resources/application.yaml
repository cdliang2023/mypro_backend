server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://192.168.2.19:3306/mydata?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: tliangty
    password: 111111
    driver-class-name: com.mysql.jdbc.Driver   # 注意类名变化
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 30MB

mybatis:
  configuration:
    map-underscore-to-camel-case: true

paper:
  root-path: 'F:/Test/vueTest/paper'

admin:
  manage:
      name: admin
      phone: '15961253171'